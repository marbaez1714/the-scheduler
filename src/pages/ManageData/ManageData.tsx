import { Outlet } from 'react-router-dom';
import { Screen } from 'src/components';

const ManageData = () => {
  // - HOOKS - //

  // - STATE - //

  // - EFFECTS - //

  // - ACTIONS - //

  // - HELPERS - //

  // - JSX - //
  return (
    <Screen>
      <Outlet />
    </Screen>
  );
};

export default ManageData;

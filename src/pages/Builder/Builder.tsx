import { Outlet } from 'react-router-dom';
import { Screen } from 'src/components/Screen';

const Builder = () => {
  // - HOOKS - //

  // - STATE - //

  // - EFFECTS - //

  // - ACTIONS - //

  // - HELPERS - //

  // - JSX - //
  return (
    <Screen title="Builders">
      <Outlet />
    </Screen>
  );
};

export default Builder;

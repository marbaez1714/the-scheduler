import { Outlet } from 'react-router-dom';
import { Screen } from 'src/components/Screen';

const Area = () => {
  // - HOOKS - //

  // - STATE - //

  // - EFFECTS - //

  // - ACTIONS - //

  // - HELPERS - //

  // - JSX - //
  return (
    <Screen title="Areas">
      <Outlet />
    </Screen>
  );
};

export default Area;

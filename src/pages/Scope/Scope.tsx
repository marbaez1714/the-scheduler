import { Outlet } from 'react-router-dom';
import { Screen } from 'src/components/Screen';

const Scope = () => {
  // - HOOKS - //

  // - STATE - //

  // - EFFECTS - //

  // - ACTIONS - //

  // - HELPERS - //

  // - JSX - //
  return (
    <Screen title="Scopes">
      <Outlet />
    </Screen>
  );
};

export default Scope;
